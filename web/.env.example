# =============================================================================
# Stock Portfolio Tracker - Frontend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# Base URL for the backend API
# All API requests will be made to this URL
#
# Local development:
#   REACT_APP_API_URL=http://localhost:8080
#
# Production (Render):
#   REACT_APP_API_URL=https://your-app.onrender.com
#
# Production (Fly.io):
#   REACT_APP_API_URL=https://your-app.fly.dev
#
# Production (Custom domain):
#   REACT_APP_API_URL=https://api.yourdomain.com
#
# IMPORTANT:
#   - All React environment variables MUST start with REACT_APP_
#   - Include the protocol (http:// or https://)
#   - Do NOT include trailing slash
#   - Do NOT include /api path (it's added automatically)
#   - Restart development server after changing this file
REACT_APP_API_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------

# Enable React development mode features
# Automatically set by npm start/build, but can be overridden
# NODE_ENV=development

# Disable source maps in production for smaller bundle size
# GENERATE_SOURCEMAP=false

# Configure build output directory (default: build)
# BUILD_PATH=build
