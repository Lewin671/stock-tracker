# =============================================================================
# Stock Portfolio Tracker - Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port number for the backend server to listen on
# Default: 8080
# Production: Usually set by hosting provider (Render, Fly.io)
PORT=8080

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MongoDB connection string
# 
# Local MongoDB:
#   MONGODB_URI=mongodb://localhost:27017/stock_portfolio
#
# MongoDB Atlas (Cloud):
#   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/stock_portfolio?retryWrites=true&w=majority
#
# Replace:
#   - username: Your MongoDB Atlas username
#   - password: Your MongoDB Atlas password
#   - cluster: Your cluster name
#   - stock_portfolio: Your database name
#
# Get this from: MongoDB Atlas → Clusters → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/stock_portfolio?retryWrites=true&w=majority

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# Secret key used to sign JWT tokens for authentication
# IMPORTANT: Use a strong, random string (32+ characters)
# Generate with: openssl rand -base64 32
# NEVER use the default value in production
JWT_SECRET=your-secret-key-change-this-in-production

# -----------------------------------------------------------------------------
# External API Keys
# -----------------------------------------------------------------------------

# Yahoo Finance API Key (Optional)
# Used for fetching stock data for all markets (US and Chinese stocks)
# The app works without this using free Yahoo Finance Chart API endpoints
# For production with higher rate limits, consider: https://rapidapi.com/apidojo/api/yahoo-finance1
# Leave empty if not using paid tier
YAHOO_FINANCE_API_KEY=

# ExchangeRate-API Key (Required for currency conversion)
# Used for USD/CNY currency conversion
# Get your key from: https://www.exchangerate-api.com/
# Steps:
#   1. Sign up at https://app.exchangerate-api.com/sign-up
#   2. Verify your email
#   3. Copy your API key from the dashboard
# Free tier: 1,500 requests/month
EXCHANGE_RATE_API_KEY=your-exchangerate-api-key

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Allowed origin for Cross-Origin Resource Sharing
# This should match your frontend URL exactly
#
# Local development:
#   CORS_ORIGIN=http://localhost:3000
#
# Production (Vercel):
#   CORS_ORIGIN=https://your-app.vercel.app
#
# Production (Custom domain):
#   CORS_ORIGIN=https://yourdomain.com
#
# IMPORTANT: 
#   - Include the protocol (http:// or https://)
#   - Do NOT include trailing slash
#   - Must match frontend URL exactly for authentication to work
CORS_ORIGIN=http://localhost:3000
